<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"Zadanie" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :"Data urodzenia" %><br />
    <%= f.datetime_select :starts_at %>
  </div>
  <div class="field">
    <%= f.label :"Data zakonczenia" %><br />
    <%= f.datetime_select :ends_at %>
  </div>
  <div class="field">
    <%= f.label :"Zadanie calodniowe" %><br />
    <%= f.check_box :all_day %>
  </div>
  <div class="field">
    <%= f.label :"Opis" %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit "Dodaj" %>
  </div>
<% end %>
